#import "template.typ": *

#show: project.with(
  theme: "lab",
  course: "C++项目管理与工程实践",
  name: "中期总结报告",
  author: "刘欣瓒 石耿同",
  major: "计算机科学与技术",
  teacher: "袁欣",
  date: "2026/7/14",
)

= 一、项目概述

SmartMate 是一款面向 Windows 桌面的任务规划应用，采用 C++20、Qt 6、Qt Widgets 与 SQLite 构建，严格遵循 MVVM 架构。项目以"可执行的任务计划 + 陪伴式桌宠"为核心，辅助用户管理任务与依赖关系，生成合理的执行顺序。View 层最初使用 Qt Quick/QML 实现，中期重构为 Qt Widgets，同时引入 Contract 抽象层进一步解耦 View 与 ViewModel。

截止中期节点，项目已完成以下功能模块：

- 任务 CRUD（创建、查看、编辑、软归档、恢复）
- 任务状态机（Start / Complete / Cancel / Redo / Archive / Restore 六命令转换）
- 任务依赖管理（Finish-to-Start 关系、环检测、依赖图可视化——QGraphicsView 实现）
- 智能规划（基于逾期、优先级、截止时间与拓扑排序的推荐算法）
- 批量操作（多选批量归档、恢复、永久删除，原子事务执行）
- 分类标签（8 色调色板、类别创建/编辑/删除、按类别筛选，依赖图按类别裁剪）
- 搜索与筛选（关键字 + 优先级 + 类别组合过滤）
- 逾期提醒（红色徽标与文案提示，60 秒定时刷新）
- 外观设置（绿色/蓝色主题切换、字体缩放）
- MVVM Contract 抽象层（View 依赖抽象接口，支持同一套 ViewModel 驱动 QML 和 Widgets 两套 View）

= 二、智能体的使用

// 此部分内容由团队成员另行补充

= 三、团队协作情况

== 分工安排

本项目由两名成员协作完成。在开发过程中，团队采用了"人机协同"的工作模式——人类成员负责架构决策与领域设计，智能体辅助完成编码实现、测试编写。本项目中，刘欣瓒同学主要负责核心代码的产出，石耿同同学主要负责功能性的测试验证

== 人与智能体协同工作

在整个开发周期中，团队成员与智能体形成了紧密的协作关系。具体实践如下：

#info[
  *架构决策由人主导。* MVVM 分层边界、状态机转换规则、依赖图的 Finish-to-Start 语义、批量操作的原子事务策略等核心设计均来自团队成员的讨论与判断。智能体不参与架构决策，而是提供"这个设计会导致什么问题"的逆向检验。
]

#tip[
  *编码实现由智能体高效执行。* 在架构确定后，具体的 C++ 实现由智能体完成——包括 Service 方法、ViewModel 属性绑定、Qt Widgets 组件布局。人类成员的角色转变为 Code Review：审查生成的代码是否遵守 AGENTS.md 中定义的 MVVM 边界，是否正确处理边界条件。
]

#warning[
  *测试用例由人和智能体共同设计。* 人类提出测试场景（如"取消任务不阻塞后继"、"批量归档含不合格任务整批回滚"），智能体编写具体的测试代码（Fake Repository 注入、断言编写）。手工功能测试表由人设计操作步骤，智能体补充边界场景。
]

协同效果：

+ *效率提升：* 大量重复性编码工作（如 WidgetBinding 双向同步、SQL 参数绑定、QAbstractListModel 角色映射）由智能体完成，人类专注于设计决策和质量把关。
+ *质量保障：* 智能体能够精准执行 MVVM 分层规则，在代码生成时自动检查依赖方向，避免了"随手把 SQL 写到 ViewModel"这类常见错误。
+ *知识传递：* 智能体对 Qt/C++/CMake 的最佳实践有全面掌握，能够在生成代码时推荐合适的 API 用法（如 `beginResetModel()/endResetModel()` 协议、`QDateTime::TransitionResolution::Reject` 等），团队成员在 Review 过程中同步学习。

现存的协同挑战：

+ 智能体生成测试文件无法覆盖到所有边界条件，仍需人类成员手工补充。
+ 依赖图可视化（正交折线路由、节点布局）这类算法问题，智能体的第一次输出往往不理想，需要多轮迭代。
+ 智能体对架构的理解往往不够深入，容易在实现中出现"看似合理但违反设计原则"的代码，需要人类成员在 Review 中指出。

= 四、阶段性成果展示

== 任务管理主界面

// TODO: 截图 — Widgets 主界面，包含聚焦槽「现在做」、任务卡片（QStyledItemDelegate 自绘，含状态色条、徽标式状态/优先级/逾期/类别标签、预计用时）、搜索与筛选工具栏

#figurex(image("./images/main-task-list.png"), [
  任务管理主界面（Qt Widgets 版本）。顶部为"现在做"聚焦槽，展示系统推荐的最优先任务并支持拖拽开始；中部为搜索与筛选工具栏，包含关键字搜索、优先级筛选、类别筛选和批量管理入口；下方为 QListView 任务卡片列表，卡片由 TaskItemDelegate 自绘，左侧色条表示任务状态，徽标式标签展示状态、优先级、逾期和类别信息。
])

== 任务编辑器

// TODO: 截图 — 新建任务对话框，展示标题/描述输入、优先级下拉框、状态只读展示（初始状态：待办）、类别选择下拉框、前置任务选择入口、截止时间与预计用时选择器

#figurex(image("./images/task-editor.png"), [
  任务编辑器。状态为只读展示，新建任务固定为"待办"。可设置优先级、类别、前置任务、截止时间与预计用时。类别通过下拉框选择，支持跳转到类别管理弹窗。
])

== 依赖图可视化

// TODO: 截图 — 依赖图页面（QGraphicsView），展示节点布局、正交折线箭头（绿色已满足/橙色未满足）、右侧详情面板、缩放控制、类别筛选

#figurex(image("./images/dependency-graph.png"), [
  Finish-to-Start 依赖图页面，使用 QGraphicsView + QGraphicsObject 渲染。节点按 Kahn 拓扑层级从左到右排列，边使用正交折线路由，绿色箭头表示前置已满足，橙色箭头表示未满足。右侧面板展示选中节点的直接前置、后继与阻塞原因。支持类别筛选——选中类别后核心节点高亮，跨类别邻居节点半透明显示。
])

== 状态机转换

#figurex(image("./images/state-transitions.png"), [
  任务状态机转换图。六个转换命令：Start（Todo→InProgress）、Complete（InProgress→Done）、Cancel（Todo/InProgress→Cancelled）、Redo（Done/Cancelled→Todo）、Archive（Done/Cancelled→Archived）、Restore（Archived→Todo/原状态）。核心约束：同一时间最多一个进行中；被阻塞任务不能 Start/Complete；只有 Done/Cancelled 可归档；只有 Archived 可永久删除。
])

== 批量操作

// TODO: 截图 — Widgets 批量模式，展示卡片右侧复选框、批量工具栏（已选择 N 项、全选、清空、归档所选/恢复所选/永久删除所选、退出）

#figurex(image("./images/bulk-mode.png"), [
  批量操作模式（Qt Widgets 版本）。进入批量模式后，聚焦槽和卡片操作按钮隐藏，卡片右侧出现复选框，顶部工具栏切换为批量操作栏。支持全选/清空、批量归档（活动视图）、批量恢复和批量永久删除（归档视图）。批量命令通过数据库条件 UPDATE 和事务回滚保证原子性——任一任务不合格则整批不写入。
])

== 类别管理

// TODO: 截图 — 类别管理弹窗，展示左侧类别列表（颜色圆点+名称+关联任务数）和右侧创建/编辑表单（名称输入+8 色调色板）

#figurex(image("./images/category-dialog.png"), [
  类别管理弹窗。左侧为已有类别列表，显示颜色标识、名称与关联任务数。右侧为创建/编辑表单，包含名称输入框和 8 色调色板。支持创建、编辑和删除类别，删除有任务关联的类别时自动将任务归为"未分类"。
])

#figurex(image("./images/category-dependency.png"), [
  依赖图中也可以用类别来显示。其中白色的为该类别的任务，浅色的为该类别任务绑定的相关依赖任务。"。
])

== 逾期提醒与搜索筛选

// TODO: 截图 — 展示逾期任务卡片上的红色「已逾期」徽标、搜索筛选工具栏（关键字搜索 + 优先级下拉 + 类别下拉 + 活动/归档切换）

#figurex(image("./images/overdue-and-filter.png"), [
  逾期提醒与搜索筛选。逾期的 Todo/InProgress 任务卡片在标题行显示红色"已逾期"圆角徽标，元数据行中的截止时间文字同步变红。搜索框支持关键字实时匹配标题和描述，优先级下拉和类别下拉支持独立或组合筛选，活动/归档标签切换视图。筛选条件改变时列表即时刷新，清除条件一键恢复。
])

= 五、总体心得与个人感悟

== 刘欣瓒

// todo

== 石耿同

这个项目我主要负责测试和验证工作。通过与智能体的协作，我对"测试驱动开发"有了更深刻的理解。最初我只是把测试当作"验证功能是否正确"的手段，但在项目中，我逐渐意识到测试本身也是一种设计工具——通过编写测试用例，我可以提前思考各种边界条件和异常场景，从而推动架构设计的完善。

另一个重要收获是关于 MVVM 架构的实践经验。前期理论课中我对 MVVM 只有大致印象：Model 提供数据和业务规则，ViewModel 管理命令并将数据与 View 绑定，View 负责渲染。但在实际开发中，我们发现第一版使用 QML 作为 View 层时，QML 的属性绑定机制虽然便捷，但隐式耦合了 ViewModel 的具体实现。因此中期决定将 View 层重构为 Qt Widgets，并引入 Contract 抽象层——View 只依赖纯虚接口，不直接感知具体 ViewModel 实现。这次重构让我深刻体会到：*MVVM 的核心不是某一套 UI 框架，而是依赖方向——View 不应该知道自己绑定的是哪个 ViewModel。* 通过严格遵循这一原则，不仅提高了代码的可维护性，也让同一套 ViewModel 可以同时驱动 QML 和 Widgets 两套 View，为后续的功能扩展（如桌宠独立窗口）打下了基础。
